const config = {
  content: ['./src/**/*.{html,svelte}'],

  theme: {
    container: {
      center: true,
      padding: '2rem',
    },
    extend: {},
  },
  
  plugins: [],
};

module.exports = config;
